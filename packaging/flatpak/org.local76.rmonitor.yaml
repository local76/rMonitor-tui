app-id: org.local76.rmonitor
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: rmonitor
modules:
  - name: rmonitor
    buildsystem: simple
    build-commands:
      - cargo build --release --locked
      - install -Dm755 target/release/rmonitor /app/bin/rmonitor
